#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @a: number to the checked
 * @b: number to the checked
 * Return: a + b
 */

int add(int a, int b)
{
	int c = a + b;

	return (c);
}
